{
  "name": "peek101",
  "version": "1.0.0",
  "private": true,
  "description": "El portal del cliente: su proyecto y su estado de cuenta. Un Worker de Cloudflare que sirve un HTML y le habla a suite101-api por dentro.",
  "type": "module",
  "scripts": {
    "previo": "wrangler dev --env staging",
    "prueba": "node --test pruebas/",
    "medir": "node scripts/medir.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "wrangler": "^4.131.1"
  }
}
